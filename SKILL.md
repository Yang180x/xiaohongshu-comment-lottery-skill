---
name: 小红书评论区寄样用户筛选
description: 根据小红书笔记链接，通过腾讯 BrowserSkill（bsk）操作用户已登录的真实浏览器，完整读取目标笔记与公开评论区，按「真实需求 > 评论相关度 > 产品匹配度 > 账号质量 > 内容价值 > 粉丝数」的优先级筛选适合寄样/体验官/试用招募的用户；依次进入候选用户主页获取公开账号数据并二次筛选，动态递补至「目标人数 + 5 备选」，最终输出含正式名单、备选名单、评论筛选明细 3 个 Sheet 的 Excel 报告。当用户提供小红书笔记链接并要求筛选寄样用户、招募体验官、筛选试用候选人时使用本 skill。
version: 1.1
author: 小杨 (WorkBuddy Skill)
last_updated: 2026-08-27
agent_created: true
---

# 小红书评论区寄样用户筛选

从一篇小红书笔记的公开评论区中，筛选出**对该笔记内容/产品真正感兴趣、与目标人群匹配、具备真实体验或种草价值**的用户，用于寄样、体验官招募或试用活动。

**核心目标不是抽人，也不是按粉丝数排序。** 综合优先级恒定为：

> 真实需求 > 评论相关度 > 产品匹配度 > 账号质量 > 内容价值 > 粉丝数量

粉丝数仅作辅助因素，绝不作为主要筛选依据。

## 输入参数

每次执行前动态向用户获取，**不得写死**：

| 参数 | 必填 | 默认 | 说明 |
|------|------|------|------|
| `note_url` | 是 | — | 小红书笔记链接 |
| `target_count` | 是 | — | 正式寄样人数 |
| `backup_count` | 否 | 5 | 备选人数 |

数量关系：`candidate_count = target_count + backup_count`。例如目标 10 人则输出 15 人完整分析结果（前 10 正式、后 5 备选）。除非用户特别指定，始终额外准备 5 位备选。

若用户消息中已给出链接和人数，直接解析使用，不重复追问。

## 前置条件与浏览器工具

**本 skill 强制优先使用 Tencent BrowserSkill（bsk CLI）**，所有网页数据必须来自真实浏览器页面。浏览器操作规范见 `browser-skill` skill（已安装于 `~/.workbuddy/skills/browser-skill/`）。

执行前检查：

```bash
bsk status --json    # 确认 extension connected ≥ 1
```

- 扩展未连接 → 停止并提示用户安装扩展（Chrome Web Store / Edge Add-ons 搜索 BrowserSkill），等弹窗变绿后再继续。
- bsk 不可用且无法恢复时，向用户说明，不得改为模型自行猜测数据；可与用户协商改为「用户提供评论Excel」走离线模式（参考 `小红书评论抽奖` skill）。

**强制会话生命周期**（任何一步出错也要走到 stop）：

```
bsk session start                 → 捕获 4 位会话 id
… 所有命令带 --session <id> …
bsk session stop <id>             → 结束时必须执行
```

## 执行流程（7 个阶段）

```
用户输入链接+人数
→ 阶段1 笔记分析（产品画像）
→ 阶段2 完整读取评论区
→ 阶段3 清洗+评论评分
→ 阶段4 建立预筛候选池（≈2倍）
→ 阶段5 逐个主页分析+二次筛选（动态递补）
→ 阶段6 综合排名+正式/备选名单
→ 阶段7 生成 Excel 交付
```

### 阶段 1：分析目标笔记

```bash
bsk navigate <note_url> --session <id>
bsk snapshot --session <id>
```

登录/验证码处理：若页面出现登录弹窗、验证码、人机验证，**不得尝试绕过**。用 `bsk request-help --session <id> --prompt "请在浏览器中完成登录/验证后点击完成" --timeout 5m` 请求用户人工处理，结果为 `continued` 后重新 `bsk snapshot` 再继续。

从快照中尽可能获取：笔记标题、正文、作者昵称、产品或主题、品类、主要卖点、使用场景、目标人群、图片/视频核心内容、可见的点赞/收藏/评论数。

基于以上信息先建立**笔记/产品画像**，明确回答：

- 这篇笔记讲什么、推荐/讨论什么、解决什么需求
- 适合什么样的人
- 什么样的评论代表真实兴趣
- 什么样的评论用户适合本次寄样

**画像未建立前，不得进入用户筛选。** 画像写入结果 JSON 的 `note_summary`。

### 阶段 2：完整读取评论区

不要只读热门/首屏/前几十条。**首选方案（2026-08 实战验证）：单次 `bsk evaluate` 挂 async 循环在页面内完成「滚动→等待→采集→去重」**，比多轮快照省一个量级的 token：

```bash
bsk evaluate --session <id> "(async () => {
  const cont = document.querySelector('.note-scroller');   // 小红书真实滚动容器(非.comments-container)
  window.__collected = window.__collected || new Map();
  const collect = () => {
    document.querySelectorAll('.comment-item').forEach(item => {
      const a = item.querySelector('a');
      const name = item.querySelector('.author-wrapper')?.textContent.trim();
      const content = item.querySelector('.content')?.textContent.trim();
      if (!name || !content) return;
      window.__collected.set(name + '||' + content.slice(0,50), {
        name, href: a ? a.href : '',
        content, time: item.querySelector('.date')?.textContent.trim() || '',
        like: (item.querySelector('.like')?.textContent || '0').replace(/[^0-9]/g,'') || '0',
        isSub: String(item.className).includes('comment-item-sub')
      });
    });
  };
  collect();
  let stagnant = 0;
  for (let i = 0; i < 45 && stagnant < 3; i++) {        // 连续3轮无新增即停
    cont.scrollTop = cont.scrollHeight;
    await new Promise(r => setTimeout(r, 1300));
    const before = window.__collected.size;
    collect();
    stagnant = window.__collected.size === before ? stagnant + 1 : 0;
  }
  return JSON.stringify({total: window.__collected.size});
})()"
# 采集完成后一次性取出:
bsk evaluate --session <id> "JSON.stringify([...window.__collected.values()])" > comments.json
```

**小红书 DOM 关键选择器（实测 2026-08）**：

| 用途 | 选择器 |
|------|--------|
| 滚动容器 | `.note-scroller` |
| 单条评论 | `.comment-item`（回复为 `.comment-item comment-item-sub`） |
| 评论昵称 | `.author-wrapper` |
| 时间+IP | `.date` |
| 评论内容 | `.content` |
| 评论点赞 | `.like` |

注意事项：
- 虚拟滚动会移除滚出视口的评论，Map 去重键 = `昵称+内容前50字`（skill 的「用户ID+评论内容」优先键在无 ID 时降级用此键）
- 快照输出可能被浏览器插件（如「社媒助手」）的 CSS 污染——保存到文件再 grep 过滤，不要直接读 stdout
- 页面上「导出评论」等插件按钮不可依赖（实测点击无效）
- 若 evaluate 方案失败，降级为多轮「`bsk press End` → `bsk wait-ms 1500` → `bsk snapshot`」循环，判断标准同为连续 3 轮无新增评论

结束条件（满足其一）：
  A. 页面明确显示「没有更多评论」「到底了」等结束状态
  B. 连续 3 次滚动后评论数、用户数、评论内容均无任何新增

因平台限制无法继续时，**如实记录实际读取条数**，不得假装读全。

每条评论尽可能保存：用户昵称、用户 ID、用户主页链接、评论内容、评论时间、评论点赞数、一级/回复标识、父级评论内容（如有）、抓取时间。

**评论去重**（页面重复渲染不重复统计）：
- 优先键：`用户ID + 评论内容`
- 无用户 ID 时：`用户名 + 评论内容 + 评论时间`

**用户去重**：筛选单位是用户不是评论。同一用户多条评论合并到该用户名下统一分析——多次高质量互动是更强的真实兴趣信号。

**访问节奏**：评论滚动每轮间隔 ≥1.5s；出现「IP存在风险」「安全限制」等风控提示时立即停止浏览器操作，报告用户并等待处理，严禁继续尝试。

### 阶段 3：无效评论清洗 + 评论评分

**清洗排除**：纯表情、哈哈哈、来了、路过、打卡、支持、666 等无实义互动；与笔记完全无关；明显广告/引流；复制粘贴模板；疑似机器人；单纯与其他评论者聊天且与笔记无关。**不得仅凭评论长度判断**——以下短评属高价值：多少钱 / 哪里买 / 求链接 / 敏感肌可以吗 / 适合油皮吗 / 可以试试吗。

每位有效评论用户按 100 分制评分：

| 维度 | 分值 | 判断要点 |
|------|------|----------|
| A 笔记相关度 | 0–35 | 是否围绕产品、功效、使用、购买、场景、人群、真实体验 |
| B 产品兴趣/需求强度 | 0–30 | 询价、问购买渠道、问是否适合自己、表达需求、想试用、被种草、问使用方式 |
| C 用户需求匹配度 | 0–20 | 评论透露的需求与产品目标人群是否吻合 |
| D 评论真实性 | 0–15 | 自然、具体、有真实语义、非模板、非广告、非机器人 |

`comment_score = A + B + C + D`。各维度打分锚点、高低分示例见 `references/评分与筛选细则.md`。

### 阶段 4：预筛候选池

为给主页二次筛选留出空间，评论阶段按分数从高到低建立**约 2 倍**的预筛候选池：

`pre_candidate_count ≈ candidate_count × 2`（如需 15 人 → 预筛约 30 人）

该值**不是死数**，按评论区实际规模动态调整：有效用户不足时（如仅 22 人）直接全量分析，不得为凑数放入明显低质量账号。

### 阶段 5：主页分析与二次筛选（动态递补）

按评论分从高到低，依次访问预筛用户主页。**推荐批量方式（实战验证）**：用 Python subprocess 循环调 `bsk navigate` + `bsk evaluate` 提取，每页间隔 8 秒，30 个主页约 6 分钟无风控：

```bash
bsk navigate <用户主页链接> --session <id>
bsk evaluate --session <id> "
(() => {
  const r = {};
  r.title = document.title.replace(' - 小红书','');
  r.desc = document.querySelector('.user-desc')?.textContent.trim() || null;
  const ints = [...document.querySelectorAll('.user-interactions span')].map(e=>e.textContent.trim());
  // ints 序列: [关注数,'关注',粉丝数,'粉丝',获赞与收藏,'获赞与收藏']
  const n = {};
  for (let i=0;i<ints.length-1;i++){
    if (ints[i+1].includes('关注') && !n.follows) n.follows = ints[i];
    if (ints[i+1].includes('粉丝') && !n.fans) n.fans = ints[i];
    if (ints[i+1].includes('获赞') && !n.likes) n.likes = ints[i];
  }
  r.nums = n;
  r.notes = [...document.querySelectorAll('.note-item')].slice(0,10).map(e =>
    (e.querySelector('.title, .name, span')?.textContent || '').trim().slice(0,40)).filter(Boolean);
  // 关注按钮状态(判断是否作者粉丝,选择器以页面实际为准)
  const btn = [...document.querySelectorAll('button, .follow-btn, [class*=follow]')]
    .map(e => (e.textContent||'').trim())
    .find(t => ['相互关注','回关','关注','已关注'].includes(t));
  r.followBtn = btn || null;
  return JSON.stringify(r);
})()"
```

| 用途 | 选择器 |
|------|--------|
| 简介 | `.user-desc`（「还没有简介」= 无简介） |
| 关注/粉丝/获赞与收藏 | `.user-interactions span`，按相邻文本配对解析 |
| 近期笔记 | `.note-item` 内 `.title/.name` |
| 关注按钮文案 | 按钮文本 ∈ {相互关注, 回关, 关注, 已关注}（选择器不固定，从快照/按钮文本匹配） |

每位用户尽可能获取**公开**数据：昵称、用户 ID、主页链接、简介、IP 属地（如公开）、粉丝数、关注数、**获赞与收藏（仅记录平台展示的合计值，禁止自行拆分或猜测）**、公开笔记数量。

条件允许时查看近期 **5–10 篇公开笔记**，判断：主要内容方向、是否产品相关垂类、是否经常分享产品、有无真实生活分享、活跃度、种草倾向、是否疑似营销号/搬运号/抽奖薅羊毛号。

- 账号定位归类：美妆 / 护肤 / 食品 / 母婴 / 穿搭 / 健身 / 家居 / 数码 / 生活方式 / 普通消费者 / 泛生活 / 其他
- 是否目标垂类：输出 **是 / 部分相关 / 否**

#### 是否为作者粉丝（硬性筛选条件）

通过候选用户主页的关注按钮状态判断（**前提：浏览器登录账号为笔记作者账号**，否则按钮反映的是当前登录人与该用户的关系，不能据此判断）：

| 按钮状态 | 是否粉丝 | 处理 |
|----------|----------|------|
| **相互关注** | 是（粉丝） | 正常保留，参与排名 |
| **回关** | 是（粉丝——该用户关注了作者） | 正常保留，参与排名 |
| **关注** | **否（非粉丝——该用户未关注作者）** | **直接淘汰，递补下一位候选** |
| 已关注 / 按钮不可见 / 其他状态 | 无法确认 | 保留，标注「无法确认」，不作为淘汰依据；不得凭评论语气猜测 |

**非粉丝一律落选**，无论评论分多高、账号质量多好——粉丝身份是本次寄样的先决条件。淘汰后按分数递补，直到凑满 `candidate_count` 位有效粉丝候选或候选耗尽。

**淘汰或降级**：**非作者粉丝（硬淘汰）**、营销号、广告号、批量搬运号、机器人、空账号、长期无内容、大量抽奖内容、明显薅羊毛、主页与产品完全无关、账号异常、无法访问且信息不足。

**动态递补**：有人淘汰 → 自动分析下一位候选，直到凑满 `candidate_count` 位有效用户或候选耗尽。不得因淘汰导致最终人数不足而不递补。

**访问节奏**：主页之间 `bsk wait-ms 8000`~`15000`，降低风控风险；单主页内容异常（风控/验证）时暂停处理，不连续硬刷。

### 阶段 6：综合排名与名单

最终排名**不能只用评论分**，综合：评论相关度、产品兴趣、需求匹配度、评论真实性、主页定位、内容质量、活跃度、是否垂类、种草价值、粉丝数、账号风险。

推荐等级：

| 等级 | 标准 |
|------|------|
| **S** | 强烈推荐：兴趣明确、需求高度匹配、账号内容高度相关 |
| **A** | 推荐：兴趣真实、账号正常、有一定内容价值 |
| **B** | 备选：兴趣真实，但内容价值/垂直度/活跃度一般 |

排名 `1~target_count` = **正式推荐**；`target_count+1 ~ candidate_count` = **备选**。永远同时输出正式+备选两份名单。

**推荐理由必须具体**，结合原始评论、体现的需求、产品匹配度、主页定位、公开内容。禁止「账号不错」「用户质量高」「推荐寄样」「粉丝较多」等空泛描述。合格示例：

> 该用户在评论中主动询问产品是否适合油皮，并表示自己长期存在夏季出油问题，与产品目标人群高度匹配。主页近期内容以护肤、美妆和生活方式为主，存在多篇产品体验类内容，具备真实体验和潜在种草价值，因此建议进入正式寄样名单。

**数量不足处理**：有效用户不足时不得凑数。如仅 12 人合格而需 15 人，输出 10 正式 + 2 备选，并明确说明「本次共发现 12 位符合当前筛选条件的有效用户，未强行补足至 15 位」。连正式人数都不足时，直接输出实际合格人数。

### 阶段 7：Excel 输出

将全部结果整理为 JSON（结构见下节），**必须调用本 skill 自带脚本生成 Excel，不得用其他方式手工拼表**：

```bash
/Users/fancy/.workbuddy/binaries/python/envs/default/bin/python \
  "<本skill目录>/scripts/generate_excel.py" <结果.json> --out <输出目录>
```

（若上述解释器不可用，退回 `python3`，需 openpyxl。）

> **排版规则（用户指定，长期有效）**：后续所有 Excel 输出一律沿用当前版本的排版布局（generate_excel.py v2.0+）。如需调整样式，只允许修改该脚本后同步版本，不允许在生成时绕过脚本另起布局。

输出文件：`小红书寄样用户筛选_YYYY-MM-DD.xlsx`，含 4 个 Sheet（总览独立成页，避免与表格列宽冲突）：

1. **筛选总览**：标题区 + 笔记/产品画像（标题、作者、产品、品类卖点、目标人群、互动数据、链接）+ 筛选漏斗（读取评论数 → 有效用户 → 预筛 → 主页分析 → 最终入选）+ 统计说明
2. **正式寄样名单**：排名、推荐等级、用户昵称、用户主页链接、原始评论、评论点赞数、评论相关度、产品兴趣度、评论评分、粉丝数、关注数、笔记数量、获赞与收藏、用户简介、账号定位、是否目标垂类、是否作者粉丝、推荐理由、风险提示
3. **备选寄样名单**：「备选排名」为首列，其余字段同正式寄样名单
4. **评论筛选明细**：用户昵称、用户主页链接、评论内容、评论点赞数、内容相关度、产品兴趣度、需求匹配度、评论真实性、评论总评分、是否进入主页分析、是否最终入选、未入选原因

表格样式（脚本内置）：紫底白字表头 + 冻结首行 + 自动筛选 + 斑马纹 + 推荐等级徽章（S橙/A绿/B灰）+ 长文本自动换行并按内容计算行高 + 数字居中/文本左对齐 + 主页链接可点击。

## 结果 JSON 数据结构

```json
{
  "note_url": "https://www.xiaohongshu.com/explore/xxx",
  "target_count": 10,
  "backup_count": 5,
  "generated_at": "2026-08-27 14:30",
  "note_summary": {
    "标题": "", "正文摘要": "", "作者昵称": "", "产品或主题": "",
    "品类": "", "主要卖点": "", "目标人群": "",
    "点赞数": "", "收藏数": "", "评论数": ""
  },
  "statistics": {
    "实际读取评论数": 0, "有效评论用户数": 0, "预筛候选数": 0,
    "主页分析数": 0, "最终入选数": 0,
    "说明": "如：评论区共读取238条评论，因平台限制未读取全部"
  },
  "final_candidates": [
    {
      "排名": 1, "推荐等级": "S", "用户昵称": "", "用户主页链接": "",
      "原始评论": "多条评论用「 | 」分隔",
      "评论点赞数": 0, "评论相关度": "高", "产品兴趣度": "高", "评论评分": 0,
      "粉丝数": 0, "关注数": 0, "笔记数量": 0, "获赞与收藏": 0,
      "用户简介": "", "账号定位": "护肤", "是否目标垂类": "是",
      "是否作者粉丝": "是（回关）/ 是（相互关注）/ 无法确认；「否」= 非粉丝已被淘汰，不会出现在最终名单",
      "推荐理由": "", "风险提示": "无",
      "名单类型": "正式", "备选排名": null
    }
  ],
  "comment_details": [
    {
      "用户昵称": "", "用户主页链接": "", "评论内容": "", "评论点赞数": 0,
      "内容相关度": 0, "产品兴趣度": 0, "需求匹配度": 0, "评论真实性": 0,
      "评论总评分": 0, "是否进入主页分析": "是", "是否最终入选": "是",
      "未入选原因": "如：非作者粉丝（主页显示「关注」）"
    }
  ]
}
```

执行过程中维护的状态对象：`note_data`、`comments`、`users`、`pre_candidates`、`analyzed_candidates`、`final_candidates`。BrowserSkill 负责真实数据采集，Agent 负责清洗/评分/排序/递补，Excel 负责结构化交付。

## 数据真实性红线（强制）

- 数据无法获取 → 填 **「无法获取」**；无法判断 → 填 **「无法确认」**
- **严禁**：编造用户/评论/粉丝数/笔记数/点赞收藏数据；猜测关注关系；猜测用户隐私；猜测平台未展示的数据
- 只使用小红书**公开可访问**的数据；不得获取或推断手机号、微信号、真实姓名、住址等非公开隐私信息
- 浏览器没拿到数据时，禁止模型自行补充或编造
- 不得绕过登录、验证码、人机验证、风控；必须人工时通过 `bsk request-help` 明确提示用户完成后再继续

## 异常处理

| 情况 | 处理 |
|------|------|
| 扩展未连接 | 提示用户安装扩展并等待，不盲目前进 |
| 登录/验证码/人机验证 | `bsk request-help` 请用户完成；禁止绕过 |
| 风控/IP 风险提示 | 立即停止浏览器操作，报告用户，等待处理 |
| 评论区加载中断 | 如实记录已读取数量，继续后续阶段 |
| 候选不足 | 按实际人数输出并明确说明，不凑数 |
| 主页无法访问 | 记录「无法获取」，标记淘汰，递补下一位 |
| bsk 命令失败 | 按退出码处理（1 参数/会话问题→修正；2 传输→`bsk doctor`；3 重试；4 加超时；5 版本不匹配→提示升级） |
| 任何结束路径 | **必须执行 `bsk session stop <id>`** |

## 与相关 skill 的关系

- `browser-skill`：本 skill 的浏览器执行层，会话/命令规范以其为准
- `小红书评论抽奖`：旧路线（评论 Excel 离线评分 + playwright-cli/Edge）；本 skill 是「笔记链接 → bsk 实时读取 → 独立 Excel」新路线。用户提供评论 Excel 时走旧 skill；提供笔记链接实时筛选时走本 skill
